# Database Configuration
# This file contains database connection settings for the POS system

# Database URL
db.url=jdbc:mysql://localhost:3306/pos_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true

# Database credentials
db.user=root
db.password=

# Connection pool settings (for future use)
db.pool.initialSize=5
db.pool.maxSize=20
db.pool.timeout=30000

# Connection settings
db.connectTimeout=5000
db.readTimeout=10000 